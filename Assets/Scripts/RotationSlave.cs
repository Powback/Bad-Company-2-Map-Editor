using UnityEngine;using System.Collections;public class RotationSlave : MonoBehaviour {	public Vector3 right;	public Vector3 up;	public Vector3 forward;	GameObject masterRotator;	// Use this for initialization	void Start () {		masterRotator = GameObject.Find ("_GM");		transform.right = right * -1;		transform.up = up;		transform.forward = forward;		//Debug.Log (transform.forward + " | " + transform.right);	}	// Update is called once per frame	void Update () {		//Vector3 rot = masterRotator.GetComponent<RotationMaster> ().calculateRotation (x,y,z);		//nx = (x * masterRotator.GetComponent<RotationMaster> ().masterMultiplier) * masterRotator.GetComponent<RotationMaster> ().xmultiplier;		//ny = (y * masterRotator.GetComponent<RotationMaster> ().masterMultiplier) * masterRotator.GetComponent<RotationMaster> ().ymultiplier;		//nz = (z * masterRotator.GetComponent<RotationMaster> ().masterMultiplier) * masterRotator.GetComponent<RotationMaster> ().zmultiplier;		//Vector3 rot = new Vector3 (nx, ny, nz);		//Vector3 rot = new Vector3 (nx, ny, nz);		//transform.rotation = Quaternion.Euler (rot);	}}