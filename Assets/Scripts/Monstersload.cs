using UnityEngine;using System.Collections;using System.Collections.Generic;public class Monstersload : MonoBehaviour {	// Use this for initialization	void Start () {		var monsterCollection = MonsterContainer.Load ("Assets/resources/monsters.xml");		foreach (Monster monster in monsterCollection.Monsters) {			Debug.Log(monster.Name + monster.Health);		}	}		// Update is called once per frame	void Update () {	}}