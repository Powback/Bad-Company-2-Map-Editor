using UnityEngine;
using System.Collections;

public class TerrainSplinePlanePointData : MonoBehaviour {

	// Use this for initialization
	void Start () {
		AssignParent();
	}

	void AssignParent() {
//		string reference = Util.GetField ("AffectedPoint", transform.GetComponent<BC2Instance> ().instance).reference;
//		if (reference != "null" && reference != "" && reference != null) {
//			GameObject parent = Util.GetGOByString (reference);
//			if (parent != null) {
//				transform.parent = parent.transform;
//			}
//		}
	}
}
