using UnityEngine;
using System.Collections;
using System.Collections.Generic;
using BC2;

public class TerrainSplinePlaneData : MonoBehaviour {
	void Start() {
		//AssignChildren ();
	}
	public void AssignChildren() {
//		Inst inst = this.gameObject.GetComponent<BC2Instance>().instance;
//		BC2Array array = Util.GetArray ("Points", inst);
//		foreach (Item item in array.item) {
//			
//			string point = Util.GetField ("AffectedPoint", Util.GetInst(item.reference, Util.GetMapload().partition)).reference;
//			Util.GetGOByString (point).gameObject.transform.parent = transform;
//
//		}
	}
}
